
public class Backstage extends State{
	private Model model;
	
	public Backstage(Model model) {
		this.model = model;

		State getDescription = "";
		State getOptions;
		State processChoice(int choice);
	}
}
